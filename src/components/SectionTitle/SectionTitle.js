// Components
import { StyledSectionTitle, StyledTitle } from "./StyledSectionTitle";

const SectionTitle = () => {
  return (
    <StyledSectionTitle>
      <StyledTitle />
    </StyledSectionTitle>
  );
};

export default SectionTitle;
